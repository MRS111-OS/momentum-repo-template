# Test directory

This directory contains all test cases for the project.

## Structure
- Create test files that mirror the src/ structure
- Use `test_*.py` naming convention
- Include fixtures and test utilities

## Running Tests
```bash
pytest tests/ -v
```
